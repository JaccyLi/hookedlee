<!-- pages/settings/settings.wxml -->
<view class="container">
  <!-- Header Section -->
  <view class="header">
    <text class="title">{{language === 'en' ? 'Settings' : '设置'}}</text>
    <text class="subtitle">{{language === 'en' ? 'Select your search engine' : '选择您的搜索引擎'}}</text>
  </view>

  <!-- Unified Section: Notice + Search Engine Selection -->
  <view class="unified-section">
    <!-- Recommendation Tip -->
    <view class="recommendation-tip">
      <text class="tip-icon">💡</text>
      <view class="tip-content">
        <text class="tip-title">{{language === 'en' ? 'Recommendation' : '推荐'}}</text>
        <text class="tip-text">{{language === 'en' ? 'Default Search offers the best balance of speed and quality for daily use.' : '默认搜索在速度和质量之间取得了最佳平衡，适合日常使用。'}}</text>
      </view>
    </view>

    <!-- Section Header -->
    <view class="section-header">
      <text class="section-title">{{language === 'en' ? 'Search Engine Selection' : '搜索引擎选择'}}</text>
      <text class="section-desc">{{language === 'en' ? 'Choose which search engine to use for finding articles' : '选择用于查找文章的搜索引擎'}}</text>
    </view>

    <!-- Search Engine Options -->
    <radio-group class="model-options" bindchange="onModelChange">
      <!-- Default Search (Smart Mode) -->
      <label class="model-card {{selectedModel === 'default' ? 'active' : ''}}">
        <radio value="default" checked="{{selectedModel === 'default'}}" class="hidden-radio" />
        <view class="model-card-content">
          <view class="model-icon-wrapper">
            <text class="model-icon">⚡</text>
          </view>
          <view class="model-details">
            <text class="model-name">{{language === 'en' ? 'Default Search' : '默认搜索'}}</text>
            <text class="model-tags">
              <text class="tag tag-quality tag-recommended">{{language === 'en' ? 'Great Quality' : '质量好'}}</text>
              <text class="tag tag-speed tag-recommended">{{language === 'en' ? 'Fast' : '快速'}}</text>
            </text>
            <text class="model-desc">{{language === 'en' ? '✅ Fast & balanced results • Best for daily use • Smart multi-engine technology' : '✅ 快速且平衡的结果 • 适合日常使用 • 智能多引擎技术'}}</text>
          </view>
          <view class="model-indicator {{selectedModel === 'default' ? 'active' : ''}}">
            <view class="indicator-dot"></view>
          </view>
        </view>
      </label>

      <!-- High Quality Search (DeepSeek-Reasoner for all) -->
      <label class="model-card {{selectedModel === 'high-quality' ? 'active' : ''}}">
        <radio value="high-quality" checked="{{selectedModel === 'high-quality'}}" class="hidden-radio" />
        <view class="model-card-content">
          <view class="model-icon-wrapper">
            <text class="model-icon">🔬</text>
          </view>
          <view class="model-details">
            <text class="model-name">{{language === 'en' ? 'High Quality Search' : '高质量搜索'}}</text>
            <text class="model-tags">
              <text class="tag tag-quality tag-best">{{language === 'en' ? 'Best Quality' : '最佳质量'}}</text>
              <text class="tag tag-speed tag-slowest">{{language === 'en' ? 'Slowest' : '最慢'}}</text>
            </text>
            <text class="model-desc">{{language === 'en' ? '✅ In-depth analysis with comprehensive results • Generates each section carefully' : '✅ 深入分析，结果全面 • 精心生成每个章节'}}</text>
          </view>
          <view class="model-indicator {{selectedModel === 'high-quality' ? 'active' : ''}}">
            <view class="indicator-dot"></view>
          </view>
        </view>
      </label>
    </radio-group>
  </view>

  <!-- Back Button -->
  <view class="button-container">
    <button class="back-btn" bindtap="goBack">
      <text class="back-btn-icon">←</text>
      <text class="back-btn-text">{{language === 'en' ? 'Back' : '返回'}}</text>
    </button>
  </view>
</view>
