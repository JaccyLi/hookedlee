<!-- pages/settings/settings.wxml -->
<view class="container">
  <!-- Header Section -->
  <view class="header">
    <text class="title">{{language === 'en' ? 'Settings' : '设置'}}</text>
    <text class="subtitle">{{language === 'en' ? 'Select your search engine' : '选择您的搜索引擎'}}</text>
  </view>

  <!-- Unified Section: Notice + Search Engine Selection -->
  <view class="unified-section">
    <!-- Recommendation Tip -->
    <view class="recommendation-tip">
      <text class="tip-icon">💡</text>
      <view class="tip-content">
        <text class="tip-title">{{language === 'en' ? 'Recommendation' : '推荐'}}</text>
        <text class="tip-text">{{language === 'en' ? 'Fast Search offers the best balance of speed and quality for daily use.' : '快速搜索在速度和质量之间取得了最佳平衡，适合日常使用。'}}</text>
      </view>
    </view>

    <!-- Section Header -->
    <view class="section-header">
      <text class="section-title">{{language === 'en' ? 'Search Engine Selection' : '搜索引擎选择'}}</text>
      <text class="section-desc">{{language === 'en' ? 'Choose which search engine to use for finding articles' : '选择用于查找文章的搜索引擎'}}</text>
    </view>

    <!-- Search Engine Options -->
    <radio-group class="model-options" bindchange="onModelChange">
      <!-- Quality Search Engine (GLM-4.7) -->
      <label class="model-card {{selectedModel === 'glm-4.7' ? 'active' : ''}}">
        <radio value="glm-4.7" checked="{{selectedModel === 'glm-4.7'}}" class="hidden-radio" />
        <view class="model-card-content">
          <view class="model-icon-wrapper">
            <text class="model-icon">📚</text>
          </view>
          <view class="model-details">
            <text class="model-name">{{language === 'en' ? 'Quality Search' : '质量搜索'}}</text>
            <text class="model-subname"></text>
            <text class="model-tags">
              <text class="tag tag-quality">{{language === 'en' ? 'Quality' : '质量好'}}</text>
              <text class="tag tag-speed">{{language === 'en' ? 'Slightly Slow' : '稍慢'}}</text>
            </text>
            <text class="model-desc">{{language === 'en' ? '✅ Comprehensive search results with detailed information' : '✅ 全面的搜索结果，信息详细'}}</text>
          </view>
          <view class="model-indicator {{selectedModel === 'glm-4.7' ? 'active' : ''}}">
            <view class="indicator-dot"></view>
          </view>
        </view>
      </label>

      <!-- Fast Search Engine (DeepSeek-Chat) -->
      <label class="model-card {{selectedModel === 'deepseek-chat' ? 'active' : ''}}">
        <radio value="deepseek-chat" checked="{{selectedModel === 'deepseek-chat'}}" class="hidden-radio" />
        <view class="model-card-content">
          <view class="model-icon-wrapper">
            <text class="model-icon">⚡</text>
          </view>
          <view class="model-details">
            <text class="model-name">{{language === 'en' ? 'Fast Search' : '快速搜索'}}</text>
            <text class="model-subname"></text>
            <text class="model-tags">
              <text class="tag tag-quality tag-recommended">{{language === 'en' ? 'Good Quality' : '质量好'}}</text>
              <text class="tag tag-speed tag-recommended">{{language === 'en' ? 'Fast' : '快速'}}</text>
            </text>
            <text class="model-desc">{{language === 'en' ? '✅ Quick results with good quality • Recommended' : '✅ 快速结果且质量好 • 推荐'}}</text>
          </view>
          <view class="model-indicator {{selectedModel === 'deepseek-chat' ? 'active' : ''}}">
            <view class="indicator-dot"></view>
          </view>
        </view>
      </label>

      <!-- Deep Search Engine (DeepSeek-Reasoner) -->
      <label class="model-card {{selectedModel === 'deepseek-reasoner' ? 'active' : ''}}">
        <radio value="deepseek-reasoner" checked="{{selectedModel === 'deepseek-reasoner'}}" class="hidden-radio" />
        <view class="model-card-content">
          <view class="model-icon-wrapper">
            <text class="model-icon">🔬</text>
          </view>
          <view class="model-details">
            <text class="model-name">{{language === 'en' ? 'Deep Search' : '深度搜索'}}</text>
            <text class="model-subname"></text>
            <text class="model-tags">
              <text class="tag tag-quality tag-best">{{language === 'en' ? 'Best Quality' : '最佳质量'}}</text>
              <text class="tag tag-speed tag-slowest">{{language === 'en' ? 'Slowest' : '最慢'}}</text>
            </text>
            <text class="model-desc">{{language === 'en' ? '✅ In-depth analysis with comprehensive results' : '✅ 深入分析，结果全面'}}</text>
          </view>
          <view class="model-indicator {{selectedModel === 'deepseek-reasoner' ? 'active' : ''}}">
            <view class="indicator-dot"></view>
          </view>
        </view>
      </label>
    </radio-group>
  </view>

  <!-- Back Button -->
  <view class="button-container">
    <button class="back-btn" bindtap="goBack">
      <text class="back-btn-icon">←</text>
      <text class="back-btn-text">{{language === 'en' ? 'Back' : '返回'}}</text>
    </button>
  </view>
</view>
